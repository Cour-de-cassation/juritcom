---
- name: Creation secrets api
  k8s:
    apply: true
    state: present
    verify_ssl: true
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: api-secret
        namespace: "{{ juritcom_namespace }}"
        labels:
          app: "{{ juritcom_application }}"
      type: Opaque
      data:
        DOC_PASSWORD: "{{ doc_password | b64encode }}"
        JWT_CLIENT_SECRET: "{{ jwt_client_secret | b64encode }}"
        JWT_SECRET: "{{ jwt_secret | b64encode }}"
...
